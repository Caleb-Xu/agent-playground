# Phase 1 - Lesson 01 学习检验
# 在下面每道题的空白处写出你的答案，不要运行，写完告诉老师
#
# 本题目对应：phase-1-tool-agent/01-agent-basics.md
# ============================================================


# ============================================================
# 第 1 题 — 推理循环
# 用自己的话（中文或伪代码都可以）描述 ReAct 循环的三个核心步骤。
# 不需要背定义，说出你的理解即可。
# ============================================================

# 你的答案：
# 步骤 1 — Thought（思考）：我想做某件事
#
# 步骤 2 — Action（行动）：我调用某个工具做上面那件事
#
# 步骤 3 — Observation（观察）：我观察做完这件事的结果
#


# ============================================================
# 第 2 题 — 工具的本质
# 下面是一个工具的定义草稿，它有什么问题？
# 请在注释里说明，并写出改正后的版本。
# ============================================================

# --- 有问题的代码 ---
def get_weather(city):
    # 假设这里调用了某个天气 API
    return {"temp": 25, "condition": "sunny"}

tool = {
    name: "get_weather",
    description: "获取指定城市的天气",
    function: get_weather,
}
# --- 有问题的代码结束 ---

# 你发现的问题（可以写多个）：
# 工具缺少参数信息，dict成员没有使用""

# 改正后的代码：
#
tool = {
    "name": "get_weather",
    "description": "获取指定城市的天气",
    "function": get_weather,
    "input_schema": {
        "type": "object",
        "properties": {
            "city": {
                "type": "string",
                "description": "查询的城市"
            }
        },
        "required": ["city"]
    }
}

# ============================================================
# 第 3 题 — Agent vs 普通函数调用
# 有人说"Agent 不就是一个大 if-else，判断用哪个工具吗？"
# 你同意这个说法吗？用 1-3 句话说明理由。
# ============================================================

# 你的答案：
# 不太同意，我们写的if-else是由我们开发者在开发代码时确定的条件，而agent是在用户输入后才做判断确定要用什么工具，我们可能会在代码中写if-else来匹配agent需要用到的工具，不过那并不是逻辑重点
